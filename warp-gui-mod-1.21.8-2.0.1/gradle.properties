org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# 编译目标：1.21.8（与运行时匹配，避免 drawTextWithShadow 等返回值变更导致的 NoSuchMethodError）
# fabric.mod.json 中的 depends 约束已设置为 >=1.21.4，向下声明兼容范围
# 但 jar 本身必须用最高目标版本编译，否则运行时方法签名不匹配会崩溃
minecraft_version=1.21.8
yarn_mappings=1.21.8+build.1
loader_version=0.18.4
fabric_version=0.136.1+1.21.8

# Mod Properties
mod_version=2.0.1
maven_group=com.warpgui
archives_base_name=warp-gui
